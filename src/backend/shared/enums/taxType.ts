export enum InvoiceTaxType {
  Inclusive = 'inclusive',
  Exclusive = 'exclusive',
  Deducted = 'deducted',
  None = 'none'
}

export enum InvoiceItemTaxType {
  Inclusive = 'inclusive',
  Exclusive = 'exclusive'
}
