import { type FC } from 'react';
import { Typography, Box } from '@mui/material';

export const StyleProfilesPage: FC = () => (
  <Box>
    <Typography variant="h4" gutterBottom fontWeight={600}>Style Profiles</Typography>
    <Typography color="text.secondary">Style profile management will be built in Phase 5.</Typography>
  </Box>
);
