import { type FC } from 'react';
import { Typography, Box } from '@mui/material';

export const ReportsPage: FC = () => (
  <Box>
    <Typography variant="h4" gutterBottom fontWeight={600}>Reports</Typography>
    <Typography color="text.secondary">Reports will be built in Phase 6.</Typography>
  </Box>
);
