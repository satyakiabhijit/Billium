import { type FC } from 'react';
import { Typography, Box } from '@mui/material';

export const PresetsPage: FC = () => (
  <Box>
    <Typography variant="h4" gutterBottom fontWeight={600}>Presets</Typography>
    <Typography color="text.secondary">Preset management will be built in Phase 6.</Typography>
  </Box>
);
