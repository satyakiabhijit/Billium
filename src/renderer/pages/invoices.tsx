import { type FC } from 'react';
import { Typography, Box } from '@mui/material';

export const InvoicesPage: FC = () => (
  <Box>
    <Typography variant="h4" gutterBottom fontWeight={600}>Invoices</Typography>
    <Typography color="text.secondary">Invoice management will be built in Phase 4.</Typography>
  </Box>
);
