import { type FC } from 'react';
import { Typography, Box } from '@mui/material';

export const LayoutsPage: FC = () => (
  <Box>
    <Typography variant="h4" gutterBottom fontWeight={600}>Layouts</Typography>
    <Typography color="text.secondary">Layout management will be built in Phase 5.</Typography>
  </Box>
);
